# Context Snapshot — PINGUI MVP

**Timestamp:** 20260626T120000Z  
**Slug:** pingui-mvp

## Task statement

Завершити та верифікувати MVP PINGUI за `3.txt` / `ROADMAP.md`: Linux PyQt6 додаток для моніторингу до 10 цілей, traceroute ICMP, in-memory статистика, топологічний граф, детекція зміни маршруту.

## Desired outcome

- Повний робочий код у `src/pingui/`
- CI зелений: ruff, mypy, pytest ≥80% coverage
- Документація актуальна (`README`, `LIVING_SPEC`, `ROADMAP`)
- Code review: APPROVE + CLEAR

## Known facts / evidence

- Реалізація Sprint 1 виконана (pyproject, src, tests, scripts, CI workflow)
- Файли ще не закомічені в git (untracked)
- venv існує, попередній CI проходив: 31 tests, 84% coverage
- Raw ICMP потребує `cap_net_raw` на Linux

## Constraints

- Python ≥3.11, тільки Linux для production ICMP
- Тестування лише у venv
- In-memory сесія (без SQLite у MVP)
- Без заглушок у production paths

## Unknowns / open questions

- Чи є cap_net_raw у поточному середовищі для network integration tests
- Чи потрібен commit/push (не запитувався)

## Likely touchpoints

- `src/pingui/icmp/`, `monitor/`, `ui/`
- `tests/`, `scripts/ci_venv.sh`
- `docs/LIVING_SPEC.md`
