# Offline country hints / IP metadata overrides (longest-prefix match).
# Not full GeoIP — see docs/pingui-geoip.md (P36). Coarse public /8 blocks and
# documentation prefixes were removed in P36-001; extend via --geoip-hints PATH.
# Legacy form: CIDR → ISO. Extended form (P36-004 YamlIpMetadataOverrides):
#   10.20.0.0/16: { country: UA, city: Kyiv, latitude: 50.45, longitude: 30.52, asn: 64512 }
# ISO 3166-1 alpha-2 country codes.
prefixes:
  # Well-known public DNS (tight /24 only)
  8.8.8.0/24: US
  8.8.4.0/24: US
  1.1.1.0/24: AU
  1.0.0.0/24: AU
  9.9.9.0/24: US
  208.67.222.0/24: US
  208.67.220.0/24: US
prefixes_v6:
  # Google Public DNS (not documentation 2001:db8::/32)
  2001:4860:4860::/48: US
